# ---- Copyright boundary: original game + derived assets stay OUT of git ----
# This repo is open-source CODE only. The original Ultimate Soccer Manager 2
# files and anything decoded from them (database, artwork) are Impressions/
# Sierra copyright and must NOT be redistributed. Provide your own copy.
# Raw original DOS game files are never redistributed (the actual game binary).
original/
*.PIC
*.DAT
*.SVE
*.PHS
*.MCH
*.THS
# Intermediate full decode (raw 144/246-byte dumps). The slim, app-ready subset
# under android/app/src/main/assets/ IS committed so the build is self-contained;
# decoded/ stays out as bulky intermediate. Regenerate: python3 tools/stage_assets.py
decoded/

# ---- Android / Gradle ----
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.apk
*.aab
*.keystore

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/

# ---- OS / editor ----
.DS_Store
*.swp
